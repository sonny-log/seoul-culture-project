<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="lib/style.css"></script>
    <title>서울시문화지도</title>
</head>
<body>
    <form action="/map">
    <div id="wrap_header">
            <div id="title_name">서울시 문화공간</div>
            <div id="map_tap">
                <div id="tap_menu">공연장</div>
                <div id="tap_menu">도서관</div>
                <div id="tap_menu">문화예술회관</div>
                <div id="tap_menu">문화원</div>
                <div id="tap_menu">미술관</div>
                <div id="tap_menu">박물관/기념관</div>
            </div>
            <div id="wrap_content">
                <div id="map_content">
                    <% for(let i=0 ; i < cul_loc.length; i++){ %> 
                    <div>
                            <li id="loc_photo"><img src="<%-cul_loc[i].main_img%>" loading="lazy"></li>
                            <li id="loc_title"><%-cul_loc[i].name%></li>
                            <li id="loc_address">주소 : <%=cul_loc[i].adress%></li>
                            <li id="loc_call">전화번호 : <%=cul_loc[i].contact%></li>
                    </div>
                    <% }; %>  
                </div>
            </div>
        </div>
    </form>>
</body>
</html>